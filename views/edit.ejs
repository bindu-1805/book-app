<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Edit Book</title>
</head>
<body>
    <h1>Edit Book</h1>
    <form action="/edit/<%= book.id %>" method="POST">
        <label>Title: <input type="text" name="title" value="<%= book.title %>" required></label>
        <label>Author: <input type="text" name="author" value="<%= book.author %>" required></label>
        <label>Rating: <input type="number" name="rating" value="<%= book.rating %>" min="1" max="5" required></label>
        <label>Review: <textarea name="review"><%= book.review %></textarea></label>
        <button type="submit">Update Book</button>
    </form>
</body>
</html>
